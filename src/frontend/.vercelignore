# ── Test & development files ─────────────────────────────
check_*.js
test_*.cjs
test_*.js
test_*.ts
tsc_output.txt
fix_*.ps1
restore_files.ps1
generate-pages.js

# ── Build artifacts (force Vercel to run buildCommand) ────
dist
dist-ssr

# ── Local Supabase CLI ───────────────────────────────────
supabase/

# ── Standalone sub-apps (not imported by main app) ────────
# NOTE: agent-panel/ and dashboards/ are imported by AppLayout.tsx
# and HomePage.tsx respectively — do NOT ignore them.
mobile/

# ── Environment secrets ──────────────────────────────────
.env
.env.local
.env.production
.env.*.local

# ── IDE / OS ─────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
