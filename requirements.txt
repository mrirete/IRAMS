# ERS — Runtime Dependencies

# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.0.0,<4.1.0  # passlib 1.7.x requires bcrypt.__about__ (removed in 4.1+)
httpx>=0.27.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# Configuration
pyyaml>=6.0

# Scheduling (Connector Manager)
apscheduler>=3.10.0,<4.0.0

# Graph / P&ID Parser
networkx>=3.2

# Image Processing (P&ID Vision)
Pillow>=10.0.0

# Database (not yet required — using in-memory stubs)
# sqlalchemy>=2.0.0,<3.0.0
# alembic>=1.13.0,<2.0.0
# asyncpg>=0.30.0,<1.0.0

# ML / Scientific (optional, for layer2 modules)
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
xgboost>=2.0.0

# AI (Gemini — server-side proxy)
google-genai>=1.0.0

# Database (Supabase — audit logging, data access)
supabase>=2.0.0

# OR-Tools (optional, for ers-work scheduling)
# ortools>=9.8
